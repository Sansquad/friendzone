import 'package:flutter/material.dart';

class ContentArchive extends StatelessWidget {
  const ContentArchive({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Archive Content'),
    );
  }
}

import 'package:flutter/material.dart';

class ContentMore extends StatelessWidget {
  const ContentMore({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('More Content'),
    );
    ;
  }
}

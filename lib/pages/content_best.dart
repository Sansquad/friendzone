import 'package:flutter/material.dart';

class ContentBest extends StatelessWidget {
  const ContentBest({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Best Content'),
    );
  }
}
